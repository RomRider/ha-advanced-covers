{
  "config": {
    "step": {
      "user": {
        "title": "Add an Advanced Cover",
        "data": {
          "wrapped_entity": "Cover to wrap"
        }
      },
      "settings": {
        "title": "Configure the Advanced Cover",
        "data": {
          "name": "Name",
          "min_value": "Minimum position",
          "max_value": "Maximum position",
          "enforce_bounds": "Proactively enforce bounds",
          "hide_wrapped_entity": "Hide the wrapped entity",
          "time_based_positioning": "Simulate absolute positioning by time",
          "open_duration": "Full open travel time",
          "close_duration": "Full close travel time",
          "skip_stop_at_limits": "Rely on the cover's own endstop at full open/close"
        },
        "data_description": {
          "min_value": "The wrapped cover will never be commanded below this position.",
          "max_value": "The wrapped cover will never be commanded above this position.",
          "enforce_bounds": "If the wrapped cover's current position is outside the bounds when they change, immediately re-command it back within bounds.",
          "hide_wrapped_entity": "Hide the wrapped cover entity in the Home Assistant UI. It remains fully available to automations and scripts.",
          "time_based_positioning": "Only shown when the wrapped cover doesn't support precise positioning but can be stopped mid-travel. Estimates its position from elapsed travel time so this Advanced Cover can still be set to a percentage.",
          "open_duration": "Time in seconds for the wrapped cover to travel from fully closed (0%) to fully open (100%).",
          "close_duration": "Time in seconds for the wrapped cover to travel from fully open (100%) to fully closed (0%).",
          "skip_stop_at_limits": "If a simulated move finishes at 0% or 100%, don't send an explicit stop command - assume the wrapped cover's hardware limit switch already stopped it. A move that finishes at any other bound always sends the stop command."
        }
      }
    },
    "error": {
      "min_greater_than_max": "Minimum position must not be greater than maximum position."
    },
    "abort": {
      "already_configured": "This cover is already wrapped by an Advanced Cover."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure an Advanced Cover",
        "data": {
          "min_value": "Minimum position",
          "max_value": "Maximum position",
          "enforce_bounds": "Proactively enforce bounds",
          "hide_wrapped_entity": "Hide the wrapped entity",
          "time_based_positioning": "Simulate absolute positioning by time",
          "open_duration": "Full open travel time",
          "close_duration": "Full close travel time",
          "skip_stop_at_limits": "Rely on the cover's own endstop at full open/close"
        },
        "data_description": {
          "min_value": "The wrapped cover will never be commanded below this position.",
          "max_value": "The wrapped cover will never be commanded above this position.",
          "enforce_bounds": "If the wrapped cover's current position is outside the bounds when they change, immediately re-command it back within bounds.",
          "hide_wrapped_entity": "Hide the wrapped cover entity in the Home Assistant UI. It remains fully available to automations and scripts.",
          "time_based_positioning": "Only shown when the wrapped cover doesn't support precise positioning but can be stopped mid-travel. Estimates its position from elapsed travel time so this Advanced Cover can still be set to a percentage.",
          "open_duration": "Time in seconds for the wrapped cover to travel from fully closed (0%) to fully open (100%).",
          "close_duration": "Time in seconds for the wrapped cover to travel from fully open (100%) to fully closed (0%).",
          "skip_stop_at_limits": "If a simulated move finishes at 0% or 100%, don't send an explicit stop command - assume the wrapped cover's hardware limit switch already stopped it. A move that finishes at any other bound always sends the stop command."
        }
      }
    },
    "error": {
      "min_greater_than_max": "Minimum position must not be greater than maximum position."
    }
  },
  "services": {
    "set_min_value": {
      "name": "Set minimum position",
      "description": "Update an Advanced Cover's minimum position bound at runtime, without reconfiguring the integration.",
      "fields": {
        "value": {
          "name": "Value",
          "description": "The new minimum position (0-100)."
        },
        "enforce": {
          "name": "Enforce",
          "description": "Override the entity's configured proactive-enforcement setting for this call only. Leave unset to use the entity's default, set to true to force an immediate re-clamp, or false to skip it."
        }
      }
    },
    "set_max_value": {
      "name": "Set maximum position",
      "description": "Update an Advanced Cover's maximum position bound at runtime, without reconfiguring the integration.",
      "fields": {
        "value": {
          "name": "Value",
          "description": "The new maximum position (0-100)."
        },
        "enforce": {
          "name": "Enforce",
          "description": "Override the entity's configured proactive-enforcement setting for this call only. Leave unset to use the entity's default, set to true to force an immediate re-clamp, or false to skip it."
        }
      }
    },
    "set_enforce_bounds": {
      "name": "Set enforce bounds",
      "description": "Update an Advanced Cover's proactive-enforcement setting at runtime, without reconfiguring the integration.",
      "fields": {
        "enforce": {
          "name": "Enforce",
          "description": "Whether the wrapped cover should be proactively re-clamped whenever it ends up outside the configured bounds."
        }
      }
    }
  }
}
